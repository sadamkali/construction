document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("[data-builders-form]").forEach(form=>{form.addEventListener("submit",async e=>{const endpoint=form.action||"",status=form.querySelector("[data-form-status]"),button=form.querySelector("button[type=submit],input[type=submit]");if(!status)return;if(endpoint.includes("YOUR_FORM_ID")){e.preventDefault();status.className="mt-3 alert alert-warning";status.textContent="The form is ready, but its email connection still needs to be added.";return}e.preventDefault();if(button)button.disabled=true;status.className="mt-3";status.textContent="Sending your message…";try{const r=await fetch(endpoint,{method:"POST",body:new FormData(form),headers:{Accept:"application/json"}});if(!r.ok)throw Error();form.reset();status.className="mt-3 alert alert-success";status.textContent="Thank you. Your message has been sent to Builders."}catch(err){status.className="mt-3 alert alert-danger";status.textContent="We could not send your message right now. Please try again or use the contact details on this page."}finally{if(button)button.disabled=false}})})});